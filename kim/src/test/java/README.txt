This is a temporary file so that we can check in the /kim/src/test/java folder 
and keep it on the classpath so that CVS allows the folder to stick around.

This README file can be deleted once we have non-folder content under this 
directory structure some place.